apiVersion: v1
kind: Service
metadata:
  name: ideaforge
  namespace: ideaforge
  labels:
    app: ideaforge
spec:
  type: ClusterIP
  selector:
    app: ideaforge
  ports:
    - name: health
      port: 3000
      targetPort: 3000
