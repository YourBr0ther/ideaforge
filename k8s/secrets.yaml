apiVersion: v1
kind: Secret
metadata:
  name: ideaforge-secrets
  namespace: ideaforge
type: Opaque
stringData:
  # GitHub tokens - one per account
  GITHUB_TOKEN_PERSONAL: "ghp_REPLACE_ME"    # YourBr0ther
  GITHUB_TOKEN_CORPORATE: "ghp_REPLACE_ME"   # ProtoForgeAI
  GITHUB_TOKEN_SPICY: "ghp_REPLACE_ME"       # DrFlirtashi
  # Discord
  DISCORD_BOT_TOKEN: "REPLACE_ME"
  DISCORD_CLIENT_ID: "REPLACE_ME"
  # Alert channel for health notifications
  ALERT_CHANNEL_ID: "REPLACE_ME"
